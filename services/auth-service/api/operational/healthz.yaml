get:
  tags:
    - operational
  summary: Service process health.
  description: Checks whether the auth-service process is alive. External dependencies are not checked here.
  responses:
    "200":
      description: Service process is alive.
      content:
        application/json:
          schema:
            $ref: ../../../../contracts/common/components.yaml#/components/schemas/HealthResponse
