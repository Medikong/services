openapi: 3.1.0
info:
  title: Auth Service API
  version: 0.1.0
  description: Auth service skeleton API. Business authentication routes are intentionally deferred.
paths:
  /healthz:
    $ref: ./operational/healthz.yaml
  /readyz:
    $ref: ./operational/readyz.yaml
  /metrics:
    $ref: ./observability/metrics.yaml
