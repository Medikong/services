get:
  tags:
    - observability
  summary: Prometheus metrics.
  description: Exposes user-service observability metrics in Prometheus text exposition format.
  responses:
    "200":
      description: Prometheus text exposition format.
      content:
        text/plain:
          schema:
            type: string
